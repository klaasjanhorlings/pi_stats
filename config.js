module.exports = {
    paths: {
        source: {
            server: './source'
        },
        config: {
            tsServer: './source/tsconfig.json'
        },
        dest: {
            build: './build'
        }
    }
}